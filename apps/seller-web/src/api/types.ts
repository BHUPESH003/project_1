/**
 * Compatibility re-export. The seller console keeps its API types in
 * `@/types/api`; some shared atoms copied from the user app import from
 * `@/api/types`, so we re-export here.
 */
export * from '@/types/api'
